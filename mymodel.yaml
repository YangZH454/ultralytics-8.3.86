# 自定义模型配置文件

#数据集文件夹datasets所在的路径
path: . # dataset root dir
#以datasets为根目录 的参与模型训练的图像文件夹
train: images/train
val: images/val
test: # test images (optional)

# 自训练模型可以识别的物体类别 以标签网站的物体标签先后顺序 依次对names字典进行赋值
names:
  0: mingren
  1: daitu
  2: woailuo